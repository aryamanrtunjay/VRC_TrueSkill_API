import { programs } from "../types.js";
export function programsEndpoints(client) {
    return {
        ...programs,
        /**
         * Get details about a program
         * @param id Program ID
         * @returns Details about the program
         */
        async get(id, options) {
            return client.GET("/programs/{id}", {
                params: { path: { id } },
                ...options,
            });
        },
        /**
         * Get a list of programs
         * @param options Query Params
         * @returns List of programs
         */
        async all(query, options) {
            return client.PaginatedGET("/programs", {
                params: { query },
                ...options,
            });
        },
    };
}
//# sourceMappingURL=programs.js.map